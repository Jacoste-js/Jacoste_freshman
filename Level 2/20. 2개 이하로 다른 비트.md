jihyukim
```js
function f(num) { 
    const i = num.lastIndexOf('0');
    const n = i + 1 < num.length ? '10' + num.substring(i + 2) : '1';
    return parseInt(num.substring(0, i) + n, 2);
}

function solution(numbers) {
    return numbers.map((el) => f('0' + el.toString(2)));
}
```
