jihyukim

```js
function solution(num) {
  return num % 2 ? 'Odd' : 'Even';
}
```

jabae

```js
function solution(num) {
  return num % 2 ? 'Odd' : 'Even';
}
```

daekim

```js
function solution(num) {
  var answer = num % 2 === 0 ? 'Even' : 'Odd';
  return answer;
}
```
